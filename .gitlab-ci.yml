pages:
  tags:
    - pages
    - docker
  image: registry.math.unistra.fr/formation-info/pages:debian-pandoc
  script:
    - pandoc -c github-pandoc.css -t html5 README.md -o index.html
    - cp logoIRMA.gif github-pandoc.css index.php public/
  artifacts:
    paths:
      - public
